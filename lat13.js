
let angka = [];

// Inisialisasi variabel untuk memulai loop
let i = 5;

// Menggunakan while loop untuk mengisi array
while (i >= 0) {
  angka.push(i);
  i--;
}

// Menampilkan hasil
console.log(angka); // Output: [5, 4, 3, 2, 1, 0]
